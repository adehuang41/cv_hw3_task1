# Object C Image to 3D

This sub-pipeline is for generating a synthetic 3D asset from a single image.

## Output convention

Write final artifacts to `outputs/` under an `object_C`-specific subdirectory when that workflow is implemented.
