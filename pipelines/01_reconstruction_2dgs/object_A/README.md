# Object A

This sub-pipeline is for reconstructing the real object captured from multiple views.

## Expected future flow

- input capture
- pose estimation / reconstruction tooling
- 2DGS-based result generation

## Output convention

Write final artifacts to `outputs/` under an `object_A`-specific subdirectory when that workflow is implemented.
