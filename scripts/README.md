# Scripts

This is the only formal script entrypoint area for reproducible execution.

- `common/`: shared helpers later if needed
- `reconstruction/`: formal reconstruction entrypoints
- `aigc/`: formal AIGC asset entrypoints
- `blender/`: formal Blender workflow entrypoints
- `evaluation/`: formal evaluation and report entrypoints

Do not create a second script system inside `pipelines/`.
